export {
    PrismaClient,
    Service,
    Appointments,
    Client,
    User,
    UserPermissions
} from '@prisma/client';

import {
    PrismaClient,
    Service,
    Appointments,
    Client,
    User,
    UserPermissions
} from '@prisma/client';


export default {
    PrismaClient
}
