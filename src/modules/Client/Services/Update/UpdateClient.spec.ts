import { ErrorApp } from '../../../../shared/Errors/Errors';
import { UpdateClient } from './UpdateClient';

describe('Update client with service', () => {
  it('Shoud be a update a Client', () => {
    expect(1).toBe(1)
  })
})